import { StyleSheet } from "react-native";


const SignUpPickApp = () => {
  return(

    <>

    </>


  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  }
});

export default SignUpPickApp;
