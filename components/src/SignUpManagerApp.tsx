import { StyleSheet } from "react-native";

const SignUpManagerApp = () => {
  return(

    <>

    </>


  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  }
});

export default SignUpManagerApp;
